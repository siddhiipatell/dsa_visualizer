version: 0.2

phases:
  install:
    commands:
      - npm install

  pre_build:
    commands:
      - npm install

  build:
    commands:
      - npm run dev


artifacts:
  files:
    - package.json
    - package-lock.json
    - "build/**/*"
  discard-paths: no
  base-directory: build